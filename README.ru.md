# kii-readiness-mapper

[![CI](https://github.com/ranas-mukminov/kii-readiness-mapper/actions/workflows/ci.yml/badge.svg)](https://github.com/ranas-mukminov/kii-readiness-mapper/actions)
[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
[![Python Version](https://img.shields.io/badge/python-3.10%2B-blue)](pyproject.toml)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

> [!IMPORTANT]
> Этот инструмент предназначен **только для самооценки** и не заменяет официальное категорирование или юридическую консультацию.

**kii-readiness-mapper** — это специализированный инструмент (CLI/Web), разработанный для помощи организациям в оценке готовности к выполнению требований Федерального закона № 187-ФЗ «О безопасности критической информационной инфраструктуры Российской Федерации» (КИИ). Он автоматизирует процесс предварительного анализа, помогая ИТ и ИБ-подразделениям идентифицировать потенциальные объекты КИИ, оценить текущие меры защиты и сформировать отчеты для внутренних аудитов.

## Основные возможности

*   **Интерактивные опросники**: CLI и веб-анкеты, адаптированные для различных отраслей (Здравоохранение, Энергетика, Финансы и др.).
*   **Оценка готовности**: Расчет вероятности отнесения организации к субъектам КИИ на основе профиля и характеристик систем.
*   **GAP-анализ**: Выявление недостающих организационных и технических мер, требуемых приказами ФСТЭК.
*   **Генерация отчетов**: Автоматическое создание подробных отчетов в форматах Markdown и HTML.
*   **Управление профилями**: Использование структурированных YAML/JSON профилей для моделирования организации и информационных систем.
*   **Расширяемая архитектура**: Модульный дизайн позволяет легко добавлять новые нормативные правила и шаблоны опросов.

## Архитектура и компоненты

Решение состоит из нескольких Python-модулей:

*   **Survey Engine**: Сбор данных через командную строку (`typer`) или веб-интерфейс (`fastapi`).
*   **Assessment Core**: Анализ собранных данных на основе набора правил, выведенных из 187-ФЗ и приказов ФСТЭК.
*   **Domain Models**: Pydantic-модели, описывающие организацию, системы и меры защиты.
*   **Report Builder**: Движок на базе Jinja2 для формирования читаемых отчетов.

## Требования

*   **ОС**: Linux, macOS или Windows.
*   **Python**: Версия 3.10 или выше.
*   **Зависимости**: `typer`, `fastapi`, `uvicorn`, `jinja2`, `pyyaml`, `pydantic` (устанавливаются автоматически).
*   **Доступ**: Права root не требуются для стандартного использования.

## Быстрый старт

1.  **Установите инструмент**:
    ```bash
    pip install kii-readiness-mapper
    ```

2.  **Инициализируйте профиль**:
    ```bash
    kii-readiness init-profile --output my-org.yaml
    ```

3.  **Запустите опрос**:
    ```bash
    kii-readiness run-cli-survey --sector health --output answers.yaml
    ```

4.  **Сгенерируйте отчет**:
    ```bash
    kii-readiness assess-profile --profile my-org.yaml --answers answers.yaml --output report.md
    ```

## Установка

### Установка через pip (Рекомендуется)

```bash
pip install kii-readiness-mapper
```

### Установка из исходного кода

Если вы хотите использовать последнюю версию из репозитория:

```bash
# Клонирование репозитория
git clone https://github.com/ranas-mukminov/kii-readiness-mapper.git
cd kii-readiness-mapper

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка пакета
pip install .
```

### Для разработки

Для установки с зависимостями для разработки (линтеры, тесты):

```bash
pip install -e ".[dev]"
```

## Настройка

Инструмент использует YAML-файлы для конфигурации и хранения данных.

### Профиль организации (`org-profile.yaml`)
Описывает юридическое лицо и основные виды деятельности.

```yaml
organization:
  name: "Моя Больница"
  inn: "1234567890"
  sector: "healthcare"
  employees_count: 500
  is_government_entity: false
```

### Файл ответов (`answers.yaml`)
Содержит сырые данные, собранные в процессе опроса.

```yaml
system_id: "his-core-01"
answers:
  has_personal_data: true
  criticality_level: "high"
  external_connections: true
  downtime_impact: "critical"
```

## Использование

### Инициализация профиля организации
Создайте шаблонный файл для описания вашей организации.

```bash
kii-readiness init-profile --output config/org.yaml
```

### Проведение опроса
Запустите интерактивную анкету в терминале для сбора данных о конкретной системе.

```bash
kii-readiness run-cli-survey \
  --sector healthcare \
  --system-type "medical-info-system" \
  --output data/survey_results.yaml
```

### Оценка готовности
Обработайте профиль и ответы для расчета показателей готовности и выявления пробелов.

```bash
kii-readiness assess-profile \
  --profile config/org.yaml \
  --answers data/survey_results.yaml \
  --out results/assessment.json
```

### Генерация отчета
Конвертируйте результаты оценки в читаемый формат (Markdown или HTML).

```bash
kii-readiness generate-report \
  --result results/assessment.json \
  --format markdown \
  --output reports/final_report.md
```

### Запуск веб-интерфейса
Запустите локальный веб-сервер для заполнения анкет через браузер.

```bash
kii-readiness run-web-survey --port 8000
# Откройте http://localhost:8000 в вашем браузере
```

## Обновление

Для обновления инструмента до последней версии:

```bash
git pull origin main
pip install . --upgrade
```

Перед обновлением проверьте `CHANGELOG.md` на наличие изменений, нарушающих обратную совместимость.

## Логи и устранение неполадок

*   **Логи**: По умолчанию инструмент выводит логи в `stderr`.
*   **Ошибки валидации**: Если вы видите `ValidationError`, проверьте ваши YAML-файлы на соответствие схеме. Сообщение об ошибке обычно указывает на конкретное поле.
*   **Отсутствующие зависимости**: Убедитесь, что вы запускаете инструмент в правильном виртуальном окружении, где был установлен пакет.
*   **Проблемы с веб-интерфейсом**: Если веб-интерфейс не загружается, проверьте, свободен ли порт 8000, или укажите другой порт с помощью `--port`.

## Замечания по безопасности

*   **Конфиденциальность данных**: Отчеты, генерируемые этим инструментом, могут содержать чувствительную информацию об уязвимостях вашей инфраструктуры. **Не публикуйте эти отчеты в открытом доступе.**
*   **Локальное выполнение**: Вся обработка происходит локально на вашем компьютере. Данные не отправляются на внешние серверы.
*   **Контроль доступа**: Ограничьте доступ к сгенерированным YAML/JSON файлам только для авторизованного персонала.

## Структура проекта

*   `src/kii_readiness_mapper/` - Основной исходный код.
    *   `assessment/` - Логика анализа пробелов и готовности.
    *   `questionnaire/` - Определения опросов и логика.
    *   `cli/` - Реализация интерфейса командной строки.
*   `scripts/` - Вспомогательные скрипты для линтинга и проверок безопасности.
*   `tests/` - Модульные и интеграционные тесты.
*   `examples/` - Примеры профилей и отчетов.

## Как внести вклад

Мы приветствуем вклад в развитие проекта! Пожалуйста, ознакомьтесь с [CONTRIBUTING.md](CONTRIBUTING.md) для получения подробной информации.

1.  Сделайте форк репозитория.
2.  Создайте ветку для новой функции.
3.  Убедитесь, что все тесты проходят (`pytest`) и код проверен линтером (`scripts/lint.sh`).
4.  Отправьте Pull Request.

## Лицензия

Этот проект лицензирован в соответствии с лицензией Apache License 2.0 - подробности см. в файле [LICENSE](LICENSE).

## Автор и коммерческая поддержка

**Автор**: [Ranas Mukminov](https://github.com/ranas-mukminov)

Для внедрения в продакшене, настройки под вашу инфраструктуру, формализованного категорирования и аудита безопасности вы можете обратиться за коммерческой поддержкой на сайте **[run-as-daemon.ru](https://run-as-daemon.ru)**. Мы предлагаем услуги консалтинга, которые помогут вам разобраться в требованиях 187-ФЗ и построить защищенную, соответствующую нормам инфраструктуру.
